#include "db_util.h"

// can also get user by pk for function overloading
User get_user(int32_t user_id)
{
    // TODO interact with db and return the user

    // random for now
    return User();
}

User get_candidate(int32_t candidate_id)
{
    // TODO interact with db and return the user

    // random for now
    return User();
}